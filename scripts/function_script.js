function calculate_hypotenuse(x,y) {
    h = (x**2 + y**2)**(1/2) // I call the hypotenuse as h in this case.
    return h;
}
// lägg till knapp så att när man trycker på den kallar man på funktionen.